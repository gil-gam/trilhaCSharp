@model ErrorViewModel
@{
    ViewData["Title"] = "An error ocurred.";
}

@{
    if (Model == null)
    {
        <div>
            <h2>Ooops! An error has ocurred!</h2>
        </div>
    }
    else
    {
        <h1>@Html.Raw(Model.Title)</h1>
        <h2 class="text-danger">@Html.Raw(Model.Message)</h2>
    }
}